import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter.app1',
  appName: 'visualizador-kinetico-2023',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
